[mypy]
# `pylattice` is installed editable (pip install -e py). Setuptools implements
# that with a runtime import hook (__editable__*.pth -> a MetaPathFinder), which
# mypy cannot follow — it only walks real directories. Point it at the source.
mypy_path = py
python_version = 3.11
