# The Hinge lattice Python examples use only the standard library
# (socket, math, time, os, sys) — no third-party packages required.
#
# They import `pylattice`, which lives one directory up. Install it first:
#
#     pip install -e ../..

# Except where noted:
#   inferno.py  — mido (MIDI input)
#   artnet.py   — stupidartnet (Art-Net receive)
stupidartnet>=1.6.0
