# The Hinge lattice Python examples use only the standard library
# (socket, math, time, os, sys) — no third-party packages required.
#
# py.lib is imported from the repo root (see the sys.path line in wave.py).
